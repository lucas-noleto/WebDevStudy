function calculadora (x,y){
    soma = x + y
    sub = x - y
    mult = x * y

    return console.log(
    `   Soma: ${soma}
    Subtração: ${sub}      
    Multiplicação: ${mult}` )
}

calculadora(4,2)
